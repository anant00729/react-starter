/**
 * @description Constants used more than once in feature module(s)
 * Always use `SNAKE_CASE_FORMAT` for declaring any constant
 */

/**
 * @description One place to edit the z-indexes for all the components in app
 * @example Always add multiples of 10 in the JSON to maintain a consistent arithmetic progression series across the DOM
 */
export const ZINDEX_LEVELS = {
  1: 10,
  2: 20,
  3: 30,
};
